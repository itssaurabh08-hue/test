ignoredBuiltDependencies:
  - sharp
  - unrs-resolver

overrides:
  # GHSA-ggr8-5vv4-36mx (stack exhaustion) — pulled in transitively by
  # prisma/@prisma/config; force the patched version until prisma bumps
  # its own dependency.
  deepmerge-ts: ">=8.0.0"

onlyBuiltDependencies:
  - "@prisma/client"
  - "@prisma/engines"
  - prisma
  - esbuild
  - msgpackr-extract
